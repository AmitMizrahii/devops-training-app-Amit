generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = "postgresql://AmitIsADevopsMan:WhatAreYouTalkingAbout69@devops-training-infraamit.cruoqks8s42q.us-east-1.rds.amazonaws.com"
}

enum Role {
  USER @map("user")
  ADMIN @map("admin")
}

model users {
  id    BigInt @id @default(autoincrement())
  email String  @unique
  name  String
  phone String @unique
  password String
  role Role @default(USER)
  createdAt  DateTime   @default(now())
  updatedAt  DateTime?  @updatedAt
}
